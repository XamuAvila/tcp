const PUSH_AIRCRAFT_POSITION = "PUSH_AIRCRAFT_POSITION";
const PUSH_AIRCRAFT = "PUSH_AIRCRAFT";

const SENSOR_CONFIG = `
    <sensorsConfig>
        <id>EO</id>
        <model>ModelXPTO</model>
        <type>EO</type>
        <uuid>uuid</uuid>
    </sensorsConfig>
`;

module.exports = { SENSOR_CONFIG, PUSH_AIRCRAFT, PUSH_AIRCRAFT_POSITION };
